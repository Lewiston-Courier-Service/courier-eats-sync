name: Cloudflare Worker Check

on:
  push:
    branches: ["main"]
  pull_request:
    branches:
      - "main"
      - "fix/square-payment-reconciliation"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm install

      - name: Run unit tests
        run: npm test

      - name: Validate Square Sandbox harness syntax
        run: node --check scripts/square-sandbox-e2e.mjs

      - name: Validate Uber Direct live quote harness syntax
        run: node --check scripts/uber-direct-live-quote.mjs

      - name: Validate corporate front-end JavaScript syntax
        run: node --check dist/app.js

      - name: Validate dispatch center front-end syntax
        run: node --check dist/dispatch-center-app.js

      - name: Validate Square corporate provisioning script syntax
        run: node --check scripts/square-provision-corporate-delivery-items.mjs

      - name: Validate Delivery-Link D1 migration locally
        shell: bash
        run: |
          rm -rf .wrangler/state
          npx wrangler d1 execute courier-eats-dispatch --local --file=test/fixtures/dispatch-schema-pre-005.sql
          npx wrangler d1 execute courier-eats-dispatch --local --file=migrations/005-corporate-delivery-link.sql
          SCHEMA="$(npx wrangler d1 execute courier-eats-dispatch --local --command="PRAGMA table_info('dispatch_orders');")"
          for column in restaurant_order_id delivery_postal_code ordering_mode delivery_fee_cents pricing_basis uber_quote_id uber_quote_fee_cents uber_quote_expires_at; do
            echo "$SCHEMA" | grep -q "$column" || { echo "Missing migrated column: $column"; exit 1; }
          done

      - name: Validate Dispatch Center D1 migration locally
        shell: bash
        run: |
          npx wrangler d1 execute courier-eats-dispatch --local --file=migrations/006-lcs-dispatch-center.sql
          SCHEMA="$(npx wrangler d1 execute courier-eats-dispatch --local --command="PRAGMA table_info('dispatch_orders');")"
          for column in shipment_category claim_status claim_deadline claimed_at claimed_by fallback_provider fallback_status fallback_attempted_at intake_key; do
            echo "$SCHEMA" | grep -q "$column" || { echo "Missing Dispatch Center column: $column"; exit 1; }
          done

      - name: Validate Cloudflare Worker
        run: npx wrangler deploy --dry-run

      - name: Validate isolated Square webhook test Worker
        run: npx wrangler deploy --dry-run --config wrangler.square-webhook-test.jsonc
